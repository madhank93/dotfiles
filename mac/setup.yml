- hosts: localhost
  name: Macbook Setup
  connection: local
  vars:
    brew_bin: "{{ '/opt/homebrew/bin/brew' if ansible_architecture == 'arm64' else '/usr/local/bin/brew' }}"
  tasks:
    # brew bundle + brew pin are run in install.sh (needs live terminal for
    # cask .pkg installers that require interactive sudo).

    - name: Get the path to ZSH
      ansible.builtin.command: which zsh
      register: zsh_path

    - name: Ensure homebrew zsh is in allowed shells
      ansible.builtin.lineinfile:
        path: /etc/shells
        line: "{{ zsh_path.stdout }}"
      become: true

    - name: Set ZSH as the default shell
      ansible.builtin.shell: "chsh -s {{ zsh_path.stdout }} {{ lookup('env', 'USER') }}"
      become: true
      ignore_errors: true
      # ignore_errors: zsh is already default on macOS Catalina+
